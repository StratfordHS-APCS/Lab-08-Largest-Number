import java.io.IOException;
import java.io.File;
import java.util.Scanner;

public class LargestNumber
{
    public static int getLargest( String line )
    {
        // Idea: use a new Scanner with the String "line" as the source.
        // Using the scanner read through the string and return the largest value.
    }

    public static void main( String args[] ) throws IOException
    {
        // Create a Scanner to read from the file "lines.dat"
        // The first line of the file has the number of data lines following.
        // Process each line, then output the line and which is largest.
    }
}
